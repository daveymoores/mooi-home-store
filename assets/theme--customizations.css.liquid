{% comment %}
  Here's an example of a customization
  You can paste this at the bottom of the page to see how it works

  /* Author - Date - Customization */
  /* E.g. Troop Themes - 12th November .... - Changed paragraph colors */
  @media only screen and (max-width: 767px) { /* mobile */
    body {
      color: blue;
    }
  }

  @media only screen and (min-width: 768px) { /* desktop */
    body {
      color: green;
    }
  }
{% endcomment %}

/* Add your theme customizations below */

#insta-feed, .instafeed-shopify {
	margin-top: 0px!important;
}


/* timer styles for black friday */
#epb_background {
	top: 0;
    border-top: 8px solid var(--bdr-color--page);
    left: 8px!important;
    width: calc(100% - 16px)!important;
}

@media (min-width: 768px) {
	#epb_background {
      border-top-width: 12px;
      left: 12px!important;
      width: calc(100% - 24px)!important;
	}
}

.announcement--root {
	background: var(--bdr-color--page);
    color: var(--bg-color--announcement);
}

.template--index .announcement--wrapper {
    padding: 8px 16px;
}

.announcement--wrapper {
    padding: 4px 16px 8px;
}

@media (min-width: 768px) {
    .template--index .announcement--wrapper {
        padding: 12px 16px;
    }

    .announcement--wrapper {
        padding: 8px 16px 12px;
    }
}
/* end timer styles for black friday */

/** payment icons **/
.mooi--payment-icons {
    display: flex;
    justify-content: center;
    padding: 8px 0px;
}

.mooi--payment-icons .payment-icons--list {
    display: flex;
    justify-content: center;
}

.mooi--payment-icons .payment-icons--item:first-of-type {
    padding-left: 0;
}

.mooi--payment-icons .payment-icons--item:last-of-type {
    padding-right: 0;
}
/** end payment icons **/

/** variant button styling **/
.product-page--variants .radios--input[type='radio']:checked + .radios--value-button {
    border-color: var(--bdr-color--page);
    background-color: #fff2f1;
}

/** end variant button styling **/

/** product page tabs **/
/*
 CSS for the main interaction
*/
.tabset > input[type="radio"] {
    position: absolute;
    left: -200vw;
}

.tabset .tab-panel {
    display: none;
}

.tabset > input:first-child:checked ~ .tab-panels > .tab-panel:first-child,
.tabset > input:nth-child(3):checked ~ .tab-panels > .tab-panel:nth-child(2),
.tabset > input:nth-child(5):checked ~ .tab-panels > .tab-panel:nth-child(3),
.tabset > input:nth-child(7):checked ~ .tab-panels > .tab-panel:nth-child(4),
.tabset > input:nth-child(9):checked ~ .tab-panels > .tab-panel:nth-child(5),
.tabset > input:nth-child(11):checked ~ .tab-panels > .tab-panel:nth-child(6) {
    display: block;
}

/*
 Styling
*/

.tabset > label {
    position: relative;
    display: inline-block;
    padding: 15px 15px;
    border: 1px solid transparent;
    border-bottom: 0;
    cursor: pointer;
    font-weight: 600;
}
.tabset > label:hover,
.tabset > input:focus + label {
    color: var(--color--body--light);
}

.tabset > label:hover::after,
.tabset > input:focus + label::after,
.tabset > input:checked + label::after {
    background: #06c;
}

.tabset > input:checked + label::after {
    content: "";
    width: 100%;
    height: 4px;
    border-radius: 3px;
    position: absolute;
    bottom: -2px;
    left: 0;
    background-color: #000;
}

.tab-panel {
    padding: 30px 0;
    border-top: 1px solid #ccc;
}
/** end product p[age tabs **/